2025-12-16 更新：
- 优化积分排名查询接口：`GET /api/v1/points/top-ranking`，移除 top 参数的 100 上限限制，现在只要求 top >= 1，支持查询任意数量的排名，提升查询灵活性。
- 优化积分记录查询接口：`GET /api/v1/points/top-n-adjust-reason-and-points-and-create-time-records-by-student-info-id-order-by-adjust-points-desc`，移除 top 参数的 100 上限限制，现在只要求 top >= 1，支持查询任意数量的改分记录。
- 优化签到排名查询接口：`GET /api/v1/attendance/top-ranking`，移除 top 参数的 100 上限限制，现在只要求 top >= 1，支持查询任意数量的签到排名，满足大规模数据查询需求。
- 更新接口文档：同步更新积分相关接口文档和签到相关接口文档，修改参数说明，移除 "范围1-100" 描述，改为 "最少为 1"。

