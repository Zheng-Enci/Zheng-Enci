2025-12-21 更新：
- 新增超级管理员接口：`GET /api/v1/super-admin/validate-special-password`，用于验证特殊密码是否正确，支持参数 `special-password`（必填），公开接口，无需认证。
- 更新特殊密码接口文档：在 `z_Note/接口文档/特殊密码接口文档.md` 中新增超级管理员验证特殊密码接口的详细文档说明，包含请求参数、响应示例、业务规则和使用示例。
- 更新特殊密码接口文档中的接口统计数量，从6个增加到7个。
|- 新增头像动态调整大小功能：实现 `getAvatarResourceByStudentInfoIdAndAvatarSize` 方法，支持默认64x64和自定义尺寸头像返回，使用内存处理避免临时文件存储。