2025-12-11 更新：
- 接口 `/api/v1/students/all-with-special-password` 返回体改为 `StudentInfoWithIdVO`，新增表主键 `id` 字段，并同步接口文档说明与示例。
- 补充接口文档 1.11 描述，明确响应模型为 `StudentInfoWithIdVO`（含主键 id）。
- 文档与日志文件（接口文档、统一认证接口文档、规则说明、日志）统一迁移至 `z_Note/` 目录，保持仓库结构整洁。
- 新增积分管理接口：`POST /api/v1/points`，通过请求头 `X-Special-Password` + 负载 `AdjustPointsDTO` 创建积分调整记录；README 已补充积分管理板块及接口统计。
- 修复 Spring Security 配置：在 `SecurityConfig.java` 中添加 `/api/v1/points/**` 路径到允许列表，解决前端无法访问积分接口的问题。
- IP白名单更新：在 `application.yaml` 中为签到验证码接口添加新的IP地址 `10.0.45.230` 到白名单。

